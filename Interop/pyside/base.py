"""This adds the ability to get the class instance before opening window"""
class BaseWidget(object):
    name = 'widget'  # Instanciated class needs to change this to something unique upon initialation.

